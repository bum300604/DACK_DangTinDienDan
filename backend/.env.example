PORT=3000
JWT_SECRET=thay-bang-chuoi-bi-mat-dai
MONGODB_URI=mongodb://bumtumu_db_user:MAT_KHAU@cluster0-shard-00-00.gyxrdsn.mongodb.net:27017,cluster0-shard-00-01.gyxrdsn.mongodb.net:27017,cluster0-shard-00-02.gyxrdsn.mongodb.net:27017/dangtin_diendan?ssl=true&replicaSet=THAY_REPLICA_SET&authSource=admin&retryWrites=true&w=majority
MONGODB_REPLICA_SET=

# Tên hiển thị trong email (mặc định: DienDanWeb)
APP_NAME=DienDanWeb

# Gửi OTP quên mật khẩu — bắt buộc SMTP_HOST + SMTP_USER + SMTP_PASS (trừ khi bật chế độ dev bên dưới)
# Gmail: SMTP_HOST=smtp.gmail.com, bật xác minh 2 bước và dùng "Mật khẩu ứng dụng" (không phải mật khẩu đăng nhập)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
MAIL_FROM=your-email@gmail.com
# Nếu SMTP treo trên host (request không trả JSON): đã có timeout mặc định; có thể chỉnh (ms)
# SMTP_CONNECTION_TIMEOUT_MS=20000
# SMTP_GREETING_TIMEOUT_MS=20000
# SMTP_SOCKET_TIMEOUT_MS=30000
# SMTP_SEND_TIMEOUT_MS=45000
# Một số SMTP không dùng STARTTLS chuẩn trên 587 — thử: SMTP_REQUIRE_TLS=false
# Chỉ môi trường dev: không có SMTP_HOST nhưng vẫn muốn test OTP — in mã ra console server
# SMTP_CONSOLE_OTP=true
